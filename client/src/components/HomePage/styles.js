import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  container: {
    paddingTop: "4rem",
  },
  special: {
    backgroundColor: "white",
    color: "#00b894",
    borderRadius: "10px",
    marginBottom: "10px",
  },
}));
