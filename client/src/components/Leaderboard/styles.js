import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  container: {
    paddingTop: "6rem",
  },
  special: {
    backgroundColor: "#00b894",
    color: "white",
    borderRadius: "10px",
    marginBottom: "10px",
  },
}));
